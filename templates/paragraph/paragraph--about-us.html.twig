<section class="about-us-sec bg-white">
<div class="container">
  <div class="row">
    <div class="col-md-6">
      <div class="about-images">
        {% for item in paragraph.field_image %}  
        <img src="{{ file_url(item.entity.uri.value )}}" class="img-fluid" alt="">
        {% endfor %}
      </div>
    </div>
      <div class="col-md-6">
      {{ content.field_about_ref }}
      </div>
  </div>
</div>
</section>